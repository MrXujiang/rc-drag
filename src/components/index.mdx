---
name: Drag
route: /Drag
order: 5
sidebar: true
---

import { Playground } from 'docz'
import Drag from './index'

# rc-drag
#### 默认

<Playground>
    <div id="box" style={{position: 'relative', width: '500px', height: '260px', border: '1px solid #06c'}}>
        <Drag container="#box" size={[200, 200]} zIndex={2} onDragStart={(item) => console.log('start', item)} onDragStop={(item) => console.log('stop', item)}><p>Mr xu</p></Drag>
        <Drag container="#box" isStatic={true}><p>static item</p></Drag>
        <Drag container="#box"><p>dragable dooring</p></Drag>
    </div>
</Playground>

## 用法

``` js
// 导入
import { Drag } from '@xlab/rc-drag'

<div id="box">
    <Drag container="#box">123</Drag>
    <Drag 
      container="#box" 
      pos={[0, 50]}
      size={[100, 100]}
    >
      <img src="https://github.com/MrXujiang/h5-Dooring/raw/master/public/logo.png" />
    </Drag>
</div>


```

## API文档
|  参数  |  说明  |  类型  |  默认值  |
|  ---   |  ---  |  ---  |  ---  |
|  container  |  画布元素或者画布id (Canvas element or canvas id) |  HTMLElement | string  | document.body |
|  pos  |  画布初始化坐标 (The canvas initializes the coordinates)  |  [x:number, y:number]  |  [0, 0]  |
|  isStatic  |  是否禁止拖拽(Whether drag is prohibited)  | boolean |  false  |
|  zIndex  |  拖拽元素的层级 (Drag the level of the element)  |  number  |  1  |
|  close  |  todo  |  -  |  -  |
|  onClose  |  todo  |  -  |   - |
